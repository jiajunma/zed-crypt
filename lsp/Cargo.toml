[package]
name = "zed-crypt-lsp"
version = "0.1.0"
edition = "2021"
description = "LSP + formatter making armored gpg/age files transparently editable in Zed"
license = "MIT"

[dependencies]
serde_json = "1"

[profile.release]
strip = true
